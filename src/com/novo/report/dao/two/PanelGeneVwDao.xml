<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.novo.report.dao.two.PanelGeneVwDao">

	<select id="getGeneSymbolByPanelName" resultType="PanelGeneVw">
		select gene_symbol from panel_gene_vw
		<where>
			 product_id = #{product_id}
			<if test="category!=null and category!=''">
				and category = #{category} 
			</if>
		</where>
		order by category desc
	</select>
	<select id="getCategoryByProductId" resultType="String">
		SELECT DISTINCT category FROM panel_gene_vw WHERE category='chemical' and product_id=#{productId}
	</select>
</mapper>